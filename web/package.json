{
  "name": "uncertainty-sky",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/vite.mjs --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "node scripts/vite.mjs preview --host 127.0.0.1",
    "test": "node --experimental-strip-types --test tests/*.test.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
